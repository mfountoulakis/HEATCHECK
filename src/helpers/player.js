function Player(name, image_url, playerRank, teamName) {
    this.name = name;
    this.image_url = image_url;
    this.playerRank = playerRank;
    this.teamName = teamName;
}

export default Player;